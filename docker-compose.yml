services:
  bot:
    build: .
    restart: unless-stopped
    # host network para acceder a PostgreSQL en 127.0.0.1:5432 del VPS
    network_mode: host
    environment:
      DATABASE_URL: ${DATABASE_URL}
    env_file:
      - .env
