# Copia este archivo como .env y completa los valores

# =============================================================
# PostgreSQL (REQUERIDO para produccion/VPS)
# =============================================================
DATABASE_URL=postgresql://usuario:password@127.0.0.1:5432/btc_predictor

# =============================================================
# Recoleccion de datos
# =============================================================
# true  = escribe datos en la DB (produccion/VPS)
# false = no escribe datos, solo ejecuta el bot (desarrollo local)
ENABLE_DATA_COLLECTION=false

# =============================================================
# URL de la DB de produccion para entrenamiento
# =============================================================
# Cuando entrenas el modelo en local, usa esta URL para leer
# datos directamente de la DB de produccion en el VPS.
# Si no se define, el trainer usa DATABASE_URL.
TRAINING_DATABASE_URL=postgresql://usuario:password@ip_vps:5432/btc_predictor

# =============================================================
# Telegram (notificaciones y comandos)
# =============================================================
TELEGRAM_TOKEN=tu_bot_token_aqui
TELEGRAM_CHAT_ID=tu_chat_id_aqui

# =============================================================
# Polymarket — Trading real (Fase 4)
# =============================================================
# POLY_PRIVATE_KEY: la private key de tu wallet MetaMask.
#   Para obtenerla: MetaMask > click en 3 puntos > Account Details >
#   Show Private Key > ingresar password > copiar.
#   Puede ser con o sin el prefijo 0x.
#
# POLY_FUNDER_ADDRESS: tu direccion publica de MetaMask (0x...).
#   Es la direccion que ves en la parte superior de MetaMask.
#   Copiala haciendo click en ella.
#
# Las API keys se derivan automaticamente a partir de POLY_PRIVATE_KEY.
# No necesitas generarlas manualmente.
# En Dokploy: configura estas variables en la seccion Environment.
POLY_PRIVATE_KEY=
POLY_FUNDER_ADDRESS=

# =============================================================
# Configuracion del bot
# =============================================================
BOT_INITIAL_CAPITAL=1000
BOT_MIN_CONFIDENCE=0.55
BOT_MAX_RISK=0.05
BOT_KELLY_FRACTION=0.35

# Daily loss limit: si se pierde este % en un dia, el bot pasa
# automaticamente a paper mode. Default: 10%
BOT_DAILY_LOSS_LIMIT_PCT=10
